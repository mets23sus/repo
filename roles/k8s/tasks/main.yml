---
# /nextcloud-ansible/roles/nginx/tasks/main.yml

- name: Создаем директорию
  file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'
  become: yes

- name: GPG key
  shell:   shell: curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.33/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
  become: yes
  

- name: Add Kubernetes APT repository
  apt_repository:
    repo: "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.33/deb/ /"
    state: present
    filename: kubernetes
    update_cache: yes
  become: yes

- name: Снять hold с пакетов Kubernetes
  dpkg_selections:
    name: "{{ item }}"
    selection: install
  loop:
    - kubelet
    - kubeadm
    - kubectl
  become: yes

- name: Ставим пакеты
  ansible.builtin.apt:
    name:
      - docker.io
      - ca-certificates
      - apt-transport-https
      - curl
    state: present
    update_cache: yes
  become: yes

- name: Ставим пакеты k8s
  ansible.builtin.apt:
    name:
      - kubelet
      - kubeadm
      - kubectl
    state: present
    update_cache: yes
  become: yes

- name: Запрет автообновления пакетов
  dpkg_selections:
    name: "{{ item }}"
    selection: hold
  loop:
    - kubelet
    - kubeadm
    - kubectl
  become: yes

- name: Ставим Докер в автозапуск
  service:
    name: docker
    enabled: yes
  become: yes

- name: Включаем Докер
  service:
    name: docker
    state: started
  become: yes
