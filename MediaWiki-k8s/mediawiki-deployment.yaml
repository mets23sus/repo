apiVersion: apps/v1  # Версия API.
kind: Deployment  # Тип: Deployment для stateless приложений.
metadata:
  name: mediawiki  # Имя.
  namespace: mediawiki  # Namespace.
spec:
  replicas: 1  # Количество реплик.
  selector:
    matchLabels:
      app: mediawiki  # Селектор.
  template:
    metadata:
      labels:
        app: mediawiki  # Лейблы подов.
    spec:
      initContainers:
      - name: init-mediawiki
        image: busybox:1.35
        command: ['sh', '-c', 'until pg_isready -h postgres -p 5432 -U mediawiki_user; do echo waiting for postgres; sleep 2; done;']
      containers:
      - name: mediawiki
        image: bitnami/mediawiki:1.39
        ports:
        - containerPort: 8080
        env:
        - name: MEDIAWIKI_DATABASE_HOST
          value: postgres
        - name: MEDIAWIKI_DATABASE_PORT_NUMBER
          value: "5432"
        - name: MEDIAWIKI_DATABASE_NAME
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: postgres-db
        - name: MEDIAWIKI_DATABASE_USER
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: postgres-user
        - name: MEDIAWIKI_DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: postgres-password
        - name: MEDIAWIKI_USERNAME
          value: admin
        - name: MEDIAWIKI_PASSWORD
          value: admin123
        - name: MEDIAWIKI_EMAIL
          value: admin@example.com
        - name: MEDIAWIKI_WIKI_NAME
          value: "My Wiki"
        - name: MEDIAWIKI_SERVER_HOST
          value: "mediawiki.local"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
        volumeMounts:
        - mountPath: /bitnami/mediawiki
          name: mediawiki-storage
      volumes:
      - name: mediawiki-storage
        persistentVolumeClaim:
          claimName: mediawiki-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mediawiki
  namespace: mediawiki
spec:
  selector:
    app: mediawiki
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP